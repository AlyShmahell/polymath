matrix:
  include:
    - language: cpp
      os:
      - linux
      compiler:
        - g++
      addons:
        apt:
          sources:
          - ubuntu-toolchain-r-test
          packages:
          - gcc
          - g++
          - python3
      before_install:
        - gcc --version
      script:
        - python3 test.py
      after_success:
        - echo success!